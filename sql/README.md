# Identifier Service API sql

This folder contains SQL commands for initializing tables. Instead of using tools such as Kysely CLI or otherwise defining migrations through using TypeScript this project currently opts in offering basic install commands for database using plain SQL.
