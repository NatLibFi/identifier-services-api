allOf:
  - type: object
    properties:
      id:
        type: number
      serial_publication_request_id:
        type: number
      serial_publisher_id:
        type:
          - number
          - 'null'
      title:
        type: string
      subtitle:
        type:
          - string
          - 'null'
      place_of_publication:
        type:
          - string
          - 'null'
      printer:
        type:
          - string
          - 'null'
      issued_from_year:
        type:
          - string
          - 'null'
      issued_from_number:
        type:
          - string
          - 'null'
      frequency:
        $ref: '../../enums/serial/serial-publication-frequency.yaml'
      frequency_other:
        type:
          - string
          - 'null'
      language:
        $ref: '../../enums/item-language.yaml'
      publication_type:
        $ref: '../../enums/serial/serial-publication-type.yaml'
      publication_type_other:
        type:
          - string
          - 'null'
      medium:
        $ref: '../../enums/serial/serial-publication-medium.yaml'
      medium_other:
        type:
          - string
          - 'null'
      url:
        type:
          - string
          - 'null'
      previous:
        type: array
        items:
          $ref: './serial-publication-previous-series.yaml'
      main_series:
        type: array
        items:
          $ref: './serial-publication-associated-series.yaml'
      subseries:
        type: array
        items:
          $ref: './serial-publication-associated-series.yaml'
      another_medium:
        type: array
        items:
          $ref: './serial-publication-associated-series.yaml'
      additional_info:
        type:
          - string
          - 'null'
      status:
        $ref: '../../enums/serial/serial-publication-status.yaml'
      issn_identifier:
        $ref: './issn-identifier.yaml'
      archive_entry:
        $ref: './serial-publication-archive.yaml'
  - $ref: '../common/created-metadata.yaml'
  - $ref: '../common/modified-metadata.yaml'
