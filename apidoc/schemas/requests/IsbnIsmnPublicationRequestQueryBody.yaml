type: object
properties:
  searchText:
    type: string
  state:
    $ref: '../enums/IsbnIsmnPublicationRequestState.yaml'
  publisherId:
    type: integer
    description: Used if wish to query publication requests based on the publisher associated with the request
  limit:
    type: integer
    minimum: 0
  offset:
    type: integer
    minimum: 0