post:
  operationId: serialPublisherAutocomplete
  summary: Endpoint for lightweight search that may be used in autocomplete
  security:
    - openId:
        - admin
  tags:
    - serial
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../../schemas/requests/serial/serial-publisher/serial-publisher-autocomplete-body.yaml'
  responses:
    '200':
      description: Ok
      content:
        application/json:
          schema:
            type: array
            items:
              oneOf:
                - $ref: '../../../schemas/responses/serial/serial-publisher-autocomplete.yaml'
    '401':
      description: Unauthorized
    '403':
      description: Forbidden
    '409':
      description: Conflict
    '422':
      description: Unprocessable Entity
    default:
      $ref: '../../../responses/error.yaml'
